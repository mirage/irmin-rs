[package]
name = "irmin"
version = "0.1.0"
authors = ["Zach Shipko <zachshipko@gmail.com>"]
edition = "2018"

[dependencies]
irmin-type-derive = {path = "./derive"}
ocaml-interop = {version = "0.7", optional = true}

[features]
default = ["bindings"]
bindings = ["ocaml-interop"]

[workspace]
members = [
  "derive"
]
